spring.application.name=service-app

# Server Port
server.port=8080

# Spring Data JPA Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/service_app
spring.datasource.username=root
spring.datasource.password=root

# Hibernate settings
# spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jackson.time-zone=Asia/Kolkata
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss.SSS

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JPA Entity Scanning
spring.jpa.packages-to-scan=com.polus.service.app.entities

# Logging Configuration
logging.level.org.springframework=INFO
logging.level.org.hibernate=INFO

# Optional: Configure CORS if your frontend runs on a different port
# spring.web.cors.allowed-origins=http://localhost:4200
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
